# 织布机 0.5B 对比报告：规则织布 vs 模型织印象

> 验收脚本：`scripts/weave_llm_acceptance.py`
> 日期：2026-08-17
> 临时沙漏：`/tmp/nexsandglass-tests-ieb1hin9`

## 0. 结论先行

- **代码交付已完成**：`weave_llm.py` 可插拔接入 `weave_insight`，模型失败自动回落纯规则，L0 校验通过。
- **真实 Qwen2.5-0.5B 对比未完成**：当前执行沙箱网络不可达，未发现 `.gguf` 模型和 `llama.cpp/ollama/mlx` 引擎；因此本报告不伪造“真实模型实测”。
- **模型分支验证已完成**：用本地 mock 适配器验证 `engine=llm` 的接入路径、结构化返回和 L0 隔离。
- **结论**：仅凭当前数据，不能断言“Qwen 织印象优于规则”；必须下载模型后重跑本验收脚本得到真实数据。

## 1. 验收环境

- 测试沙数量：24 条
- 话题数：8 个（Notion, 黑咖啡, 本地模型, 织布机, 深色模式, 命令行, llama.cpp, 模型织布）
- 测试目录：`/tmp/nexsandglass-tests-ieb1hin9`（`NEXSANDBASE_HOME=/tmp/xxx`，不碰真实数据）
- 模型状态：**不可达**（shell DNS/出网被沙箱阻断；无 `.gguf`；无 `llama_cpp/ollama/mlx_lm`）
- 阻断证据：`curl hf-mirror.com` DNS 失败；`curl --resolve` 连接被拒；Hermes venv 无 `torch/transformers/llama_cpp/mlx_lm/gguf`；全盘未发现 `.gguf` 模型文件

## 2. 架构红线核验

| 红线 | 结果 |
|---|---|
| 模型产出永不写回 `sandglass.txt` / L0 | ✅ `L0 哈希未变` |
| 可插拔：模型不可用/失败自动回落纯规则 | ✅ 不可达端点全部 `engine=rule` |
| 保留原 `weave_insight.synthesis` | ✅ 原字段不变，新增 `synthesis_enhanced` / `impression_*` |
| 测试只使用临时 `NEXSANDBASE_HOME` | ✅ 脚本自动创建 `/tmp/nexsandglass-tests-*` |

## 3. 量化指标

| 指标 | 规则织布（真实运行） | 模型织印象（mock 分支，非 Qwen 实测） |
|---|---|---|
| 样本数 | 8 | 8 |
| 平均延迟 ms | 0.12 | 0.0* |
| 平均实体数 | 8.0 | 5.0 |
| 平均联结数 | 5.62 | 3.12 |
| 平均句子数 | 3.25 | 2.12 |
| 平均句长（字） | 27.82 | 47.58 |
| 结构化得分 | 5.0/6 | 6.0/6 |
| RSS 增量 MB | 0 | 0* |

* mock 分支仅验证代码路径，不是 409MB Qwen 模型的真实推理延迟/内存。

## 4. 逐条对比（话题 × 规则 synthesis vs 模型 impression）

| 话题 | 规则印象片段 | 模型印象片段（mock） | 规则联结数 | 模型联结数 |
|---|---|---|---|---|
| Notion | 关于「Notion」的记忆开始浮现。关键节点：Notion、Obsidian、use | 关于「Notion」的记忆浮现出来：这些线索不是孤立的点，而是绕着本地工具偏好慢慢聚 | 2 | 2 |
| 黑咖啡 | 关于「黑咖啡」的记忆开始浮现。关键节点：user、黑咖啡、咖啡、来一杯、我决定用。已 | 关于「黑咖啡」的记忆浮现出来：这些线索不是孤立的点，而是绕着本地工具偏好慢慢聚合。像 | 1 | 1 |
| 本地模型 | 关于「本地模型」的记忆开始浮现。关键节点：本地模型、user、强制订阅的软件、买断制 | 关于「本地模型」的记忆浮现出来：这些线索不是孤立的点，而是绕着本地工具偏好慢慢聚合。 | 10 | 5 |
| 织布机 | 关于「织布机」的记忆开始浮现。关键节点：织布机、user、强制订阅的软件、买断制、命 | 关于「织布机」的记忆浮现出来：这些线索不是孤立的点，而是绕着本地工具偏好慢慢聚合。像 | 10 | 5 |
| 深色模式 | 关于「深色模式」的记忆开始浮现。关键节点：user、深色模式、咖啡、来一杯、我决定用 | 关于「深色模式」的记忆浮现出来：这些线索不是孤立的点，而是绕着本地工具偏好慢慢聚合。 | 1 | 1 |
| 命令行 | 关于「命令行」的记忆开始浮现。关键节点：user、命令行、咖啡、来一杯、我决定用。已 | 关于「命令行」的记忆浮现出来：这些线索不是孤立的点，而是绕着本地工具偏好慢慢聚合。像 | 1 | 1 |
| llama.cpp | 关于「llama.cpp」的记忆开始浮现。关键节点：llama.cpp、user、强 | 关于「llama.cpp」的记忆浮现出来：这些线索不是孤立的点，而是绕着本地工具偏好 | 10 | 5 |
| 模型织布 | 关于「模型织布」的记忆开始浮现。关键节点：模型织布、user、强制订阅的软件、买断制 | 关于「模型织布」的记忆浮现出来：这些线索不是孤立的点，而是绕着本地工具偏好慢慢聚合。 | 10 | 5 |

## 5. 不可达端点回落抽查

| 话题 | 引擎 | 回落原因片段 | 印象非空 |
|---|---|---|---|
| Notion | rule | URLError:<urlopen error [Errno 1] Operation not permitted> | True |
| 黑咖啡 | rule | URLError:<urlopen error [Errno 1] Operation not permitted> | True |
| 本地模型 | rule | URLError:<urlopen error [Errno 1] Operation not permitted> | True |
| 织布机 | rule | URLError:<urlopen error [Errno 1] Operation not permitted> | True |

## 6. 使用说明

### 下载模型

```bash
export HF_ENDPOINT=https://hf-mirror.com
hf download Qwen/Qwen2.5-0.5B-Instruct-GGUF qwen2.5-0.5b-instruct-q4_0.gguf \
  --local-dir ~/.nexsandglass/models
```

### 开关

| 环境变量 | 作用 | 默认 |
|---|---|---|
| `NEXSANDBASE_LLM_ENABLED` | `auto/1/0`，模型织印象总开关 | `auto` |
| `NEXSANDBASE_LLM_GGUF` | GGUF 路径（自动走 `llama_cpp`） | 空 |
| `NEXSANDBASE_LLM_ENDPOINT` | OpenAI 兼容本地端点（llama-server/Ollama/LM Studio） | 空 |
| `NEXSANDBASE_LLM_MODEL` | 端点模型名 | `qwen2.5-0.5b-instruct` |
| `NEXSANDBASE_LLM_OLLAMA_MODEL` | Ollama 原生模型名 | 空 |
| `NEXSANDBASE_LLM_TIMEOUT` | 调用超时秒 | `30` |

### 本地启动示例

```bash
# llama.cpp server（推荐，OpenAI 兼容）
llama-server -m ~/.nexsandglass/models/qwen2.5-0.5b-instruct-q4_0.gguf \
  --host 127.0.0.1 --port 8080 -c 4096 -ngl -1
export NEXSANDBASE_LLM_ENDPOINT=http://127.0.0.1:8080/v1
export NEXSANDBASE_LLM_MODEL=qwen2.5-0.5b-instruct
export NEXSANDBASE_LLM_ENABLED=auto
```

### 重跑真实模型对比

```bash
python scripts/weave_llm_acceptance.py
# 若已下载/启动真实模型，验收脚本会得到 engine=llm 的实际延迟与内存。
```

