# V3.1.0 立体像合成 + 织线补漏对比报告

> 任务单：`20260817_V3.1_立体像织线_任务单.md`
> 验收脚本：`scripts/v31_3d_wthread_acceptance.py`
> 原始 JSON：`reports/20260817_v31_acceptance.json`
> 日期：2026-08-17

## 0. 结论

- **代码交付完成**：`_synthesize_3d` 已接 `weave_llm` 本地小模型，织线补漏已实现，并保留原本地聚合/正则织线回落。
- **真实 Qwen 对比未完成**：本执行沙箱能发现 409MB 模型文件和 `llama_cpp 0.3.34`，但创建 `llama_context` 失败——`ggml_metal_init` 无法创建 command queue，属沙箱 Metal 后端限制，不伪造真实模型数据。
- **模型分支已用 mock 适配器验证**：`synthesize_3d` 与 `weave_missing_triples` 的模型路径、字段归一化、写入路径、防幻觉门控均按预期运行。
- **L0 安全通过**：规则、模型分支、不可达回落三个阶段前后 `sandglass.txt` 哈希完全一致。

## 1. 验收环境

- 临时沙漏：`/tmp/nexsandglass-v31-tests-01k0vg90`（`NEXSANDBASE_HOME=/tmp/xxx`，未触碰真实数据）
- 测试沙：20 条，覆盖工具选择、迁移、偏好、情绪、本地模型等场景
- 模型文件：`~/.nexsandglass/models/qwen2.5-0.5b-instruct-q4_0.gguf` 已发现（409MB）
- 引擎：`llama_cpp 0.3.34` 已安装
- 真实模型可用性：**否**
- 阻断证据：`Llama(...)` 输出 `ggml_metal_init: error: failed to create command queue`，最终 `Failed to create llama_context`

## 2. 架构红线核验

| 红线 | 结果 |
|---|---|
| 模型产出永不写回 `sandglass.txt` / L0 | ✅ 六次阶段哈希一致，`l0_unchanged=true` |
| 可插拔：模型不可用/失败自动回落 | ✅ `_synthesize_3d` 回落到 `3D 管道合成`；`weave_missing_triples` 回落纯正则 |
| 保留原 22 行本地聚合 / 原正则织线 | ✅ 规则分支输出与改前字段一致，仅新增可空扩展字段 |
| 测试只使用临时沙漏 | ✅ 脚本自动创建 `/tmp/nexsandglass-v31-tests-*`，结束后清理 |

## 3. 任务 A：立体像合成对比

| 指标 | 本地聚合（真实运行） | 模型合成（mock 分支） |
|---|---|---|
| `persona_type` | `本地优先工具控`（7 字） | `本地优先、买断制偏好的精简工具使用者`（18 字） |
| `reminder_tone` | `数据汇报` | `行动式` |
| `reminder_example` | 空（0 字） | `今天先把 Figma 的版本整理一下，别再同时开三个编辑器。`（30 字，1 句） |
| `source` | `3D 管道合成` | `3D 模型合成` |
| `engine` | `rule` | `llm` |

- **语气多样性**：单样本下规则和模型各 1 种；模型分支输出已归一化到 `行动式`，本地聚合仍为二值 `数据汇报/安静陪伴`。
- **示例可读性**：模型分支从空示例变为 1 句贴合场景的提醒，可读性 +30 字。
- **画像语义度**：模型分支从短标题变为“本地优先 + 买断制 + 精简工具”的语义描述，语义度 7 → 18 字。
- **不可达回落抽查**：`engine=rule`，`fallback_reason=URLError:<urlopen error [Errno 1] Operation not permitted>`，`source=3D 管道合成`。

## 4. 任务 B：织线补漏对比

| 指标 | 数值 |
|---|---|
| 纯正则织线基线 | 10 条 |
| 模型补漏候选 | 3 条 |
| 有效补入 | 2 条 |
| 补入后总数 | 12 条 |
| 三元组数量提升率 | +20.00% |
| 幻觉丢弃 | 1 条（`Photoshop`/`海报` 不在原文） |
| 非法/重复丢弃 | 0 条 |
| 不可达回落 | `engine=rule`，`added=0`，不写入 |

有效补入样例：

```text
Figma --用途--> 界面设计 (source_line=1)
Lightroom --用途--> 照片 (source_line=2)
```

防幻觉门控：

- `subject/object` 必须逐字出现在资料原文；
- `source_line` 必须能定位到同时包含主体和客体的原文行；
- 幻觉样例 `Photoshop --用途--> 海报` 被丢弃，未写入织线表。

## 5. 改动文件清单

| 文件 | 改动 |
|---|---|
| `sandglass_core/weave_llm.py` | 新增 `synthesize_3d`、`weave_missing_triples`、3D/三元组 prompt、通用模型调用参数；扩展 `__all__` |
| `sandglass_core/sandglass_think.py` | `_synthesize_3d` 在本地聚合后尝试模型增强，失败回落；新增 `reminder_example`/`synthesis_*` 字段 |
| `sandglass_core/weave_l3.py` | 新增 `weave_thread_fill` 织线补漏程序化入口 |
| `sandglass_core/weavethread.py` | 更新模块说明：正则为主，LLM 补漏经 `wthread_add` 写 L2，不写 L0 |
| `scripts/v31_3d_wthread_acceptance.py` | 新增 V3.1 验收脚本 |
| 版本号 | `sandglass_paths.py`、`install.py`、`memory_provider.py`、`agent_bootstrap.py`、`v3/sandglass_paths.py`、`plugin.yaml`、`pyproject.toml`、`setup.py`、`plugin.py`、`pulse.py` 对齐 3.1.0 |
| 文档 | `README.md`、`ARCHITECTURE.md` 增加 V3.1.0 说明 |

备份目录：`.bak-v31/`

## 6. Hermes 补跑真实模型

当前沙箱 Metal 后端不可用，建议在 Hermes 本机执行：

```bash
export NEXSANDBASE_LLM_ENABLED=1
export NEXSANDBASE_LLM_GGUF=~/.nexsandglass/models/qwen2.5-0.5b-instruct-q4_0.gguf
python scripts/v31_3d_wthread_acceptance.py
```

如需专门跑真实 Qwen，可临时把验收脚本的 mock 适配器替换为真实后端；代码本身已支持 `GGUF / OpenAI 兼容端点 / Ollama` 三种后端。
